import 'package:flutter/material.dart';

class SizeBoxH30 extends StatelessWidget {
  const SizeBoxH30({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return SizedBox(
      height:30,
    );
  }
}
